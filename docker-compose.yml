version: '3.8'

services:
  backend:
    build: ./backend
    container_name: grade-backend
    ports:
      - "9999:5000"
    volumes:
      - ./backend/data:/app/data
    networks:
      - grade-network

  frontend:
    build: ./frontend-user
    container_name: grade-frontend
    ports:
      - "8080:8080"
    depends_on:
      - backend
    networks:
      - grade-network

networks:
  grade-network:
    driver: bridge
